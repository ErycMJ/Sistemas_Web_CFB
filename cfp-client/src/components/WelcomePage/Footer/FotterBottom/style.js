import { styled } from "@stitches/react";

const FooterBottomContainer = styled("div", {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    margin: "0 20px",
})

export { FooterBottomContainer };