import { styled } from '@stitches/react';
import { colors } from '../../../shared/utils/Colors';

const WelcomeSectionContainer = styled("div", {
    backgroundColor: colors().green,
    padding: "20px"
});

export { WelcomeSectionContainer }